# BUSAD 878 — Midterm Dashboard Demo dependencies
# Recommended: Python 3.10–3.12 (3.13/3.14 are very new; some wheels lag).
# Create an isolated environment first:
#     python -m venv .venv
#     .venv\Scripts\activate      (Windows)   |   source .venv/bin/activate (mac/Linux)
#     pip install -r requirements.txt

# --- core data ---
pandas>=2.0
numpy>=1.24

# --- charts + interactive web app ---
plotly>=5.18
streamlit>=1.36

# --- generative AI: Google Gemini (provides `from google import genai`) ---
# Free API key: https://aistudio.google.com/apikey
google-genai>=0.3

# --- notebook (for midterm_dashboard_demo.ipynb) ---
jupyter>=1.0
ipywidgets>=8.0
